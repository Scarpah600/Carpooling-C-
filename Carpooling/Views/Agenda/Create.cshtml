@model Carpooling.Models.AGENDA

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>AGENDA</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.DATAHORA)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.DATAHORA)
            @Html.ValidationMessageFor(model => model.DATAHORA)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.CARRO_CARROID, "VEICULO")
        </div>
        <div class="editor-field">
            @Html.DropDownList("CARRO_CARROID", String.Empty)
            @Html.ValidationMessageFor(model => model.CARRO_CARROID)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.DESTINOFINAL_DESTINOID, "DESTINO")
        </div>
        <div class="editor-field">
            @Html.DropDownList("DESTINOFINAL_DESTINOID", String.Empty)
            @Html.ValidationMessageFor(model => model.DESTINOFINAL_DESTINOID)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.USUARIOPROPRIETARIO_ID, "USUARIOS")
        </div>
        <div class="editor-field">
            @Html.DropDownList("USUARIOPROPRIETARIO_ID", String.Empty)
            @Html.ValidationMessageFor(model => model.USUARIOPROPRIETARIO_ID)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
